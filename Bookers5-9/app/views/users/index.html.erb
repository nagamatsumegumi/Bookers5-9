<div class="container">
     <div class="row">
        <div class="col-sm-3 sidenav">

<%= render 'users/userinfo', user: current_user, book: @book %>

        </div>

<div class="col-sm-9">

<h2>Users</h2>

<table class="table">
  <thead>
  <tr>
    <th>Image</th>
    <th>Name</th>
    <th></th>
  </tr>
  </thead>


  <% @users.each do |user| %>
    <tr>
      <td><%= attachment_image_tag user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "50x50" %></td>
      <td><%= user.name %></td>
      <td><%= link_to 'show', user_path(user), class: "show_#{user.id}" %></td>
    </tr>
  <% end %>
</table>


  </div>
        </div>

</div>